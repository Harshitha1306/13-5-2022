import math
class Solution:
    def isPrime (self, N):
        arr=True
        for i in range(N):
            if N == 0 or N == 1:
                return False
            arr[0] = False
            arr[1] = False
            x=int(math.sqrt(N))
            for i in range(2,x+1):
                if x%i == 0:
                   print('it is a prime')
                else:('not')
